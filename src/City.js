import React from 'react';
import Main from './Main';
const City = ({forecast}) => {
    console.log('forecast in City: ', forecast);
    return (
        <div id="city">
            city
            
        </div>
    )
}
export default City;